import {resetProgress,configureOwned,equipWeapon,bossDefeated,takeReward,nextRound,craft,weaponFor,dropEquipment,equipmentStats,rollLevelGems,chooseLevelGem,updateWaves} from './progression.js';
import {createLinks,configureLink,compileSkill,rollGemRewards,GEM_PRESETS} from './gems.js';
import {EXTRA_ENEMIES,BOSS_ROSTER,isBoss,bossName,startExtraAttack,resolveExtraAttack} from './monsters.js';
import {updateCasting,updateProjectiles,castSkill} from './combat.js';
import {makePlayer,SKILLS,ENEMIES,MAX_ENEMIES,MAX_PARTICLES,BOSS_TIME,WORLD_SIZE,RARITIES} from './data.js';
import {seeded,createWorld,moveEntity,distance,SpatialHash,clamp} from './world.js';
/** Pure simulation. No DOM/browser dependency; renderer and UI subscribe through events. */
export class Game{
 constructor({seed=Date.now()>>>0,onEvent=()=>{}}={}){this.onEvent=onEvent;this.reset(seed);this.state='menu'}
 reset(seed=Date.now()>>>0){resetProgress(this);this.fields=[];this.totems=[];this.gemLevels={};this.castClocks=[0,.3,.6];this.echoes=[];this.meteors=[];this.effects=[];this.leechBudget=8;this.seed=seed;this.rng=seeded(seed^0xBADCAFE);this.world=createWorld(seed);this.player=makePlayer();this.enemies=[];this.shots=[];this.hostile=[];this.gems=[];this.particles=[];this.texts=[];this.zones=[];this.owned={};this.time=0;this.kills=0;this.spawnClock=0;this.fireClock=0;this.orbitClock=0;this.bossSpawned=false;this.boss=null;this.shake=0;this.flash=0;this.nextId=1;this.state='playing';this.grid=new SpatialHash(100);this.choices=[];this.onEvent('reset')}
 configure(index,kind,socket,value){const r=configureOwned(this,index,kind,socket,value);if(r.ok)this.emit('loadout');return r}
 equip(index,id){const r=equipWeapon(this,index,id);if(r.ok)this.emit('loadout');return r}
 choose(i){return chooseLevelGem(this,i)}
 takeReward(i){return takeReward(this,i)}
 nextRound(){return nextRound(this)}
 craft(id,action,options){const r=craft(this,id,action,options);if(r.ok)this.emit('loadout');return r}
 finish(){if(this.state!=='forge'||this.round!==3||this.wave!==15||!this.rewardTaken)return false;this.state='won';this.emit('end',true);return true}
 emit(type,data){this.onEvent(type,data)}
 pause(){if(this.state==='playing'){this.state='paused';this.emit('pause')}else if(this.state==='paused'){this.state='playing';this.emit('resume')}}
 dash(){const p=this.player;if(this.state!=='playing'||p.dashCooldown>0)return false;p.dashCooldown=3;p.dashTime=.18;p.invincible=.35;this.burst(p.x,p.y,'#d9dfb4',16);this.emit('sound','dash');return true}
 spawn(type,x,y){const stats=ENEMIES[type]||EXTRA_ENEMIES[type];if(!stats)throw new Error('Unknown enemy: '+type);const scale=(type==='boss'||stats.boss)?1:1+this.time/240;const e={...stats,maxHp:stats.hp*scale,hp:stats.hp*scale,type,x,y,id:this.nextId++,slow:0,hit:0,cooldown:.4+this.rng()*.6,attack:0,aim:0,phase:0};moveEntity(e,0,0,this.world);this.enemies.push(e);return e}
 spawnWave(){if(this.enemies.length>=MAX_ENEMIES)return;const p=this.player,n=Math.min(this.waveRemaining,2);for(let i=0;i<n&&this.enemies.length<MAX_ENEMIES;i++){const a=this.rng()*Math.PI*2,r=490+this.rng()*190,x=clamp(p.x+Math.cos(a)*r,65,WORLD_SIZE-65),y=clamp(p.y+Math.sin(a)*r,65,WORLD_SIZE-65),roll=this.rng();const pool=this.time>100?['crawler','runner','shaman','brute','charger','spitter','summoner','wraith']:this.time>55?['crawler','runner','shaman','charger','spitter','wraith']:this.time>22?['crawler','runner','shaman','charger']:['crawler','runner'];const type=pool[Math.floor(roll*pool.length)];this.spawn(type,x,y);this.waveRemaining--}}
 summonBoss(type=BOSS_ROSTER[Math.floor(this.rng()*BOSS_ROSTER.length)]){this.bossSpawned=true;const p=this.player,a=this.rng()*6.28;this.boss=this.spawn(type,clamp(p.x+Math.cos(a)*440,90,WORLD_SIZE-90),clamp(p.y+Math.sin(a)*440,90,WORLD_SIZE-90));this.boss.hp=this.boss.maxHp=(260+((this.round-1)*3+Math.ceil(this.wave/5))*95)*(this.boss.maxHp/2700);this.boss.damage*=.6+this.round*.025;this.emit('boss',bossName(this.boss));this.emit('sound','boss');this.shake=7}
 burst(x,y,color,count=8){for(let i=0;i<count&&this.particles.length<MAX_PARTICLES;i++){const a=this.rng()*6.28,s=25+this.rng()*110;this.particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.25+this.rng()*.35,max:.6,color,r:1+this.rng()*3})}}
 hitEnemy(e,damage,{dot=false}={}){if(e.hp<=0||this.state!=='playing')return;e.hp-=damage;if(!dot){e.hit=.12;this.burst(e.x,e.y,isBoss(e)?'#f8b768':'#c9d5a0',4)}if(!dot&&this.texts.length<70)this.texts.push({x:e.x,y:e.y-20,text:Math.round(damage),life:.65,color:'#f7d5a0'});if(!dot)this.emit('sound','hit');if(e.hp<=0){this.kills++;this.burst(e.x,e.y,'#ddba73',12);dropEquipment(this,isBoss(e));if(isBoss(e))this.player.xp+=e.xp;else if(this.gems.length<250)this.gems.push({x:e.x,y:e.y,value:e.xp,r:5});else{const g=this.gems.reduce((a,b)=>distance(a,e)<distance(b,e)?a:b);g.value+=e.xp}if(isBoss(e)){this.shake=12;bossDefeated(this)}else if(this.rng()<.05){const keys=['chromatic','jeweller','fusing'];this.bag.currency[keys[Math.floor(this.rng()*3)]]++}else if(this.rng()<.025)this.gems.push({x:e.x+8,y:e.y,value:0,heal:12,r:6})}}
 hurt(amount){const p=this.player;if(p.invincible>0||this.state!=='playing')return;p.hp=Math.max(0,p.hp-Math.max(1,amount-p.armor-equipmentStats(this).armor));p.invincible=.7;this.shake=7;this.flash=.2;this.burst(p.x,p.y,'#eb8f77',13);this.emit('sound','hurt');if(p.hp<=0){this.state='dead';this.emit('sound','die');this.emit('end',false)}}
 checkLevel(){const p=this.player;if(!['playing','reward','camp','forge'].includes(this.state)||p.xp<p.nextXp)return;this.levelReturn=this.state;p.xp-=p.nextXp;p.level++;p.nextXp=Math.round(p.nextXp*1.18+4);p.damage*=1.07;p.maxHp+=5;p.hp=Math.min(p.maxHp,p.hp+15);this.choices=rollLevelGems(this);this.state='levelup';this.emit('sound','level');this.emit('levelup',this.choices)}

 shoot(){return castSkill(this,compileSkill(this.links[0],this.player,this.gemLevels))}
 fireHostile(e,a,n=1,speed=140){for(let i=0;i<n&&this.hostile.length<220;i++){const angle=a+(i-(n-1)/2)*.20;this.hostile.push({x:e.x,y:e.y,vx:Math.cos(angle)*speed,vy:Math.sin(angle)*speed,life:5,r:isBoss(e)?8:6,damage:e.damage,color:e.color})}}
 step(dt,input={x:0,y:0}){
 if(this.state!=='playing')return;dt=Math.min(.05,Math.max(0,dt));this.time+=dt;this.roundTime+=dt;const p=this.player;this.leechBudget=Math.min(8,this.leechBudget+8*dt);
 p.invincible=Math.max(0,p.invincible-dt);p.dashCooldown=Math.max(0,p.dashCooldown-dt);p.dashTime=Math.max(0,p.dashTime-dt);p.cast=Math.max(0,p.cast-dt);p.hp=Math.min(p.maxHp,p.hp+p.regen*dt);this.shake=Math.max(0,this.shake-dt*25);this.flash=Math.max(0,this.flash-dt);
 if(Math.hypot(input.x,input.y)>.05){const len=Math.max(1,Math.hypot(input.x,input.y));p.dirX=input.x/len;p.dirY=input.y/len;p.walk+=dt*12}const dx=p.dashTime>0?p.dirX:input.x,dy=p.dashTime>0?p.dirY:input.y,vel=p.speed*(1+equipmentStats(this).speed/100)*(p.dashTime>0?3.7:1);moveEntity(p,dx*vel*dt,dy*vel*dt,this.world);if(p.dashTime>0)this.burst(p.x,p.y,'#cad9ba',2);
 this.spawnClock-=dt;if(this.wave%5!==0&&this.waveRemaining>0&&this.spawnClock<=0){this.spawnWave();this.spawnClock=Math.max(.45,1.5-this.time/200)}
 updateWaves(this);if(this.state!=='playing')return;
 updateCasting(this,dt);if(this.state!=='playing')return;
 this.grid.clear();for(const e of this.enemies)if(e.hp>0)this.grid.add(e);
 for(const e of this.enemies){if(e.hp<=0)continue;e.hit=Math.max(0,e.hit-dt);e.slow=Math.max(0,e.slow-dt);e.cooldown-=dt;if(e.poisonTime>0){e.poisonTime-=dt;this.hitEnemy(e,(e.poisonDamage||0)*dt,{dot:true});if(this.state!=='playing')return;if(e.hp<=0)continue}if(e.burnTime>0){e.burnTime-=dt;this.hitEnemy(e,(e.burnDamage||0)*dt,{dot:true});if(this.state!=='playing')return;if(e.hp<=0)continue}const dist=distance(e,p),angle=Math.atan2(p.y-e.y,p.x-e.x),boss=isBoss(e);
  if(e.charge>0){e.charge-=dt;moveEntity(e,Math.cos(e.aim)*460*dt,Math.sin(e.aim)*460*dt,this.world);if(distance(e,p)<e.r+p.r+3)this.hurt(e.damage);this.burst(e.x,e.y,'#cb9878',1);continue}
  if(e.attack>0){e.attack-=dt;if(e.attack<=0){if(resolveExtraAttack(this,e)){}else if(boss){if(e.phase%2===0){for(let k=0;k<14;k++)this.fireHostile(e,k*Math.PI/7+e.aim,1,135);this.fireHostile(e,e.aim,5,195)}else{this.zones.push({x:e.tx,y:e.ty,r:100,timer:.7,life:.35,active:false,damage:32});this.zones.push({x:clamp(e.tx+100,40,WORLD_SIZE-40),y:e.ty+80,r:65,timer:1.1,life:.35,active:false,damage:23})}e.phase++;e.cooldown=e.hp<e.maxHp*.5?1.9:2.7;this.emit('sound','boss')}else if(e.type==='shaman'){this.fireHostile(e,e.aim,3,135);e.cooldown=2.5}else{if(dist<e.r+p.r+25)this.hurt(e.damage);e.cooldown=.9}}}
  else if(e.cooldown<=0&&(startExtraAttack(e,dist)||(boss&&dist<700)||(e.type==='shaman'&&dist<370)||dist<e.r+p.r+20)){e.attack=boss?.85:['charger','spitter','summoner'].includes(e.type)?.8:e.type==='shaman'?.7:.42;e.aim=angle;e.tx=p.x;e.ty=p.y}
  else{let speed=e.speed*(e.slow>0?1-(e.slowFactor||.35):1)*(boss&&e.hp<e.maxHp*.5?1.35:1);if(['shaman','spitter','summoner'].includes(e.type)&&dist<240)speed=dist<160?-speed*.5:0;let vx=Math.cos(angle)*speed,vy=Math.sin(angle)*speed;for(const o of this.grid.query(e.x,e.y,65)){if(o===e||o.hp<=0)continue;const d=distance(e,o),min=e.r+o.r+4;if(d<min&&d>0){vx+=(e.x-o.x)/d*(min-d)*3;vy+=(e.y-o.y)/d*(min-d)*3}}if(e.type==='wraith'){e.x=clamp(e.x+vx*dt,20,WORLD_SIZE-20);e.y=clamp(e.y+vy*dt,20,WORLD_SIZE-20)}else moveEntity(e,vx*dt,vy*dt,this.world)}
 }
 // Rebuild after movement so projectile queries use current positions.
 this.grid.clear();for(const e of this.enemies)if(e.hp>0)this.grid.add(e);
 updateProjectiles(this,dt);if(this.state!=='playing')return;
 for(const s of this.hostile){s.life-=dt;s.x+=s.vx*dt;s.y+=s.vy*dt;if(distance(s,p)<p.r+s.r){this.hurt(s.damage);s.life=0}if(this.world.hash.query(s.x,s.y,50).some(o=>distance(s,o)<s.r+o.r))s.life=0}
 for(const z of this.zones){z.timer-=dt;if(z.timer<=0){z.life-=dt;z.active=true;if(distance(z,p)<z.r+p.r)this.hurt(z.damage)}}this.zones=this.zones.filter(z=>z.life>0);
 this.orbitClock-=dt;if(p.orbits&&this.orbitClock<=0){this.orbitClock=.25;for(let i=0;i<p.orbits;i++){const a=this.time*2.6+i*6.28/p.orbits,o={x:p.x+Math.cos(a)*66,y:p.y+Math.sin(a)*66};for(const e of this.grid.query(o.x,o.y,65))if(distance(e,o)<e.r+14)this.hitEnemy(e,p.damage*.55)}}
 for(const g of this.gems){const d=distance(g,p);if(d<p.magnet){const a=Math.atan2(p.y-g.y,p.x-g.x),v=Math.min(d,Math.max(160,450-d)*dt);g.x+=Math.cos(a)*v;g.y+=Math.sin(a)*v}if(distance(g,p)<p.r+10){if(g.heal)p.hp=Math.min(p.maxHp,p.hp+g.heal);else p.xp+=g.value;g.dead=true;this.emit('sound','pickup')}}
 this.gems=this.gems.filter(g=>!g.dead);this.enemies=this.enemies.filter(e=>e.hp>0);this.shots=this.shots.filter(s=>s.life>0);this.hostile=this.hostile.filter(s=>s.life>0);
 for(const q of this.particles){q.life-=dt;q.x+=q.vx*dt;q.y+=q.vy*dt;q.vx*=.95;q.vy*=.95}this.particles=this.particles.filter(q=>q.life>0);for(const t of this.texts){t.y-=dt*28;t.life-=dt}this.texts=this.texts.filter(t=>t.life>0);this.checkLevel();
 }
}
